# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=kigalipack_db

# JWT
JWT_SECRET=change-me-in-production

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Frontend (verification links)
FRONTEND_URL=http://localhost:3001
SUPPORT_EMAIL=support@kigalipack.rw

# SMTP (required for verification emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Kigali-Pack <noreply@kigalipack.rw>
